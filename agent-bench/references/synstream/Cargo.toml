[package]
name = "wavefront-bench"
version = "0.1.0"
edition = "2021"

[lib]
name = "wavefront_bench"
crate-type = ["dylib", "rlib"]

[dependencies]
synstream-types = { path = "../../synstream-types" }
synstream-macro = { path = "../../synstream-macro" }
