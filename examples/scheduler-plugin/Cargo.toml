[package]
name = "scheduler-plugin-example"
version = "0.1.0"
edition = "2021"
description = "Example external scheduler plugin for Tomii using the stable TaskScheduler trait."

[dependencies]
tomii-core = { path = "../../tomii-core", features = [] }
tomii-types = { path = "../../tomii-types" }
