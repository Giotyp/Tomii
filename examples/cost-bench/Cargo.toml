[package]
name = "cost-bench"
version = "0.1.0"
edition = "2021"

[lib]
name = "costbench"
crate-type = ["dylib", "rlib"]

[dependencies]
synstream-types = { path = "../../synstream-types" }
rayon = "1.10"
