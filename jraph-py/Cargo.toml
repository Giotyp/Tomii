[package]
name = "jraph-py"
version = "0.1.0"
edition = "2021"

[lib]
name = "pyjraph"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23.3", features = ["extension-module"] }
jraph-core = { path = "../jraph-core" }